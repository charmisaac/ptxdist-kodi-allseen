Index: utf8proc-1.3.1/Makefile
===================================================================
--- utf8proc-1.3.1.orig/Makefile
+++ utf8proc-1.3.1/Makefile
@@ -2,8 +2,8 @@
 
 # programs
 MAKE=make
-AR?=ar
-CC?=gcc
+#AR?=ar
+#CC?=gcc
 INSTALL=install
 FIND=find
 
@@ -33,7 +33,7 @@ else # GNU/Linux, at least (Windows shou
 endif
 
 # installation directories (for 'make install')
-prefix=/usr/local
+prefix=/usr
 libdir=$(prefix)/lib
 includedir=$(prefix)/include
 
